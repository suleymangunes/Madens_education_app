part of '../../init/theme/light/light_theme_custom.dart';

// Added constans for light theme
class LightThemeColors {
  const LightThemeColors._();
  static const Color broom = Color.fromRGBO(255, 237, 15, 1);
  static const Color orangePeel = Color.fromRGBO(255, 156, 0, 1);
  static const Color scarlet = Color.fromRGBO(255, 48, 0, 1);
  static const Color brightRed = Color.fromRGBO(182, 0, 0, 1);
}

class LightThemeString {
  const LightThemeString._();
}

class LightThemeIcon {
  const LightThemeIcon._();
  static const Color _iconColor = Colors.black;
}
