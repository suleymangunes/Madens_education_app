import 'package:flutter/material.dart';

extension BorderRadiusSizes on BuildContext {
  BorderRadius get searchBorder => BorderRadius.circular(20);
  BorderRadius get imageBorder => BorderRadius.circular(10);
}
